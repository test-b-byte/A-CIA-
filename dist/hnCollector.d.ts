/** Fetches current Hacker News stories. Tries the front page first. Falls back to newest stories if that fails. */
/** Fetches current Hacker News stories. Tries the front page first. Falls back to newest stories if that fails. */
export declare function fetchHnStories(): Promise<string>;
//# sourceMappingURL=hnCollector.d.ts.map